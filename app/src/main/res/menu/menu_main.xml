<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/filterMi"
        android:icon="@drawable/ic_filter"
        android:title="@string/filter"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/filterMovieNameMi"
                android:title="Filtrar por Nome " />
            <item
                android:id="@+id/filterMovieGradeMi"
                android:title="Filtrar por Nota" />
        </menu>
    </item>

    <item android:id="@+id/addMovieMi"
        android:icon="@drawable/ic_add"
        android:title="@string/add_movie"
        app:showAsAction="ifRoom"
        >
    </item>
</menu>